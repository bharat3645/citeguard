# citeguard
Offline citation-claim support verifier for research papers — flags citations that don't actually support the claim next to them, using transparent TF-IDF similarity (no external API, no black box).
